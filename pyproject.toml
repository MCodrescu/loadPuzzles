[project]
name = "chesspuzzles"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.117.1",
    "chess",
    "stockfish",
    "chess-dot-com",
    "pandas",
    "psycopg2-binary",
    "sqlalchemy",
]

[tool.vercel]
entrypoint = "api.index:app"